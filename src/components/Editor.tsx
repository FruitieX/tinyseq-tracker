import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

export default class Editor extends React.Component {
  render() {
    return <Container>foo bar</Container>;
  }
}
